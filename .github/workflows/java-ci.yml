name: Java CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    - name: Run tests
      run: mvn test
    - name: Capture Test Results
      if: failure()
      run: |
        mkdir -p ${{ github.workspace }}/test-results
        for file in ${GITHUB_WORKSPACE}/target/surefire-reports/*.xml; do
          echo "Processing $file" >> ${{ github.workspace }}/test-results/failed_tests_summary.txt
          # Extracting the names of failed tests
          cat $file | grep '<testcase' | grep -B 1 '<failure' | grep -oP '(?<=name=").*?(?=")' >> ${{ github.workspace }}/test-results/failed_tests_summary.txt
        done
    - name: Telegram Notification on Failure
      if: failure()
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TELEGRAM_CHAT_ID }}
        token: ${{ secrets.TELEGRAM_TOKEN }}
        message: "Некоторые тесты не пройдены. См. подробности."
        document: "${{ github.workspace }}/test-results/failed_tests_summary.txt"
    - name: Telegram Notification on Success
      if: success()
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TELEGRAM_CHAT_ID }}
        token: ${{ secrets.TELEGRAM_TOKEN }}
        message: "Все ок"
